Secure Auto Deployment Platform 

This project is a backend system that allows users to deploy applications automatically using Docker.

It aims to simulate a mini cloud deployment platform (like Heroku/Render) with added security features.
